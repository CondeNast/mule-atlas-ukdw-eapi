api:
  name:         "${api.artifactId}" 
  groupId:      "${api.groupId}" 
  artifactId:   "${api.artifactId}" 
  version:      "${api.version}"
  majorVersion: "${api.major.version}"
  
http:
  private:
    port: "8091"
  reconnection:
    frequency: "2000"
    attempts: "2"
  keepApiBaseUri: "true"
    
dlb:
  internal:
    protocol: "HTTPS"
    port: "443"
  
